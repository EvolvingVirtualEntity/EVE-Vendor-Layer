# Manually-installed apt packages required for the E.V.E. platform.
# Read by install.sh; passed to `apt-get install -y`.
# Standard Ubuntu defaults (bash, coreutils, login, etc.) are not listed here
# since install.sh targets stock Ubuntu 24.04 LTS.

# Build deps (Go bridges + Python compiled wheels)
build-essential
g++
make

# Standard tooling
curl
git
gnupg
ca-certificates
apt-transport-https

# Media + OCR
ffmpeg
qrencode
tesseract-ocr
tesseract-ocr-eng
tesseract-ocr-deu
wkhtmltopdf

# Languages
golang-go
python3
python3-pip
python3-venv

# Note: nodejs (v20+) comes from NodeSource repo, not stock apt — install.sh adds that.
# Note: cloudflared comes from Cloudflare's repo — install.sh adds that.
# Note: tailscale comes from Tailscale's repo — install.sh adds that.
